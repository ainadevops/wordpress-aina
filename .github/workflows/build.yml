---
name: build-wordpress-image

on:
  push:
    branches:  ["main"]

jobs:
  build:
    run-on: ubuntu-server
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: build image
        run: docker build -t wordpress:latest
    
